How to test:

1. replace the empty 'kernel' directory with the actual one
2. cd kernel
3. make
4. cp nl ..
5. cd ..
6. ./nl p.csv c.csv
7. plot 's.dat' for spikes and 'v.dat' for membrane potentials, respectively
